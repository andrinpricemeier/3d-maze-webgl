export class LightningShow {
    activate(scene) {
        // The scene is what we are applying the lightning show on.
        // Now activate multiple lights and move the lights, redrawing the scene each time.
    }
}